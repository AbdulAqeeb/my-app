import { ImpDirective } from './imp.directive';

describe('ImpDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpDirective();
    expect(directive).toBeTruthy();
  });
});
